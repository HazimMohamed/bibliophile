# Android dev builds point at a Windows-side relay so the emulator does not
# need direct Tailscale DNS/routing. The native Capacitor transport handles
# requests from the app to this local HTTP hop.
VITE_API_BASE_URL=http://10.0.2.2:8787/api
